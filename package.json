{
  "scripts": {
    "build": "zola build && pnpm tailwindcss -i ./public/index.input.css -o ./public/index.css",
    "serve": "zola serve --interface 0.0.0.0 & pnpm tailwindcss -i ./public/index.input.css -o ./public/index.css --watch && wait"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "tailwindcss": "^3.4.0"
  }
}
